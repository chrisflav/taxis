/.lake
/issues.sqlite
/issues.sqlite-wal
/issues.sqlite-shm
/.env
.env.local
docker/.env
docker/data/
# Holds credentials, like .env — config.example.toml is the committed template.
/config.toml
docker/config.toml
.claude/settings.local.json

# Python bytecode
__pycache__/
*.pyc

# Written by bench/run.py --json (and by the CI benchmark job).
bench-results.json
