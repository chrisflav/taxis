# The bundle a taxis server serves. `0` is not merely "not packaged": it is what lets the whole
# multi-server store — the list, the migration, the switcher — fold away at build time instead of
# riding along in the entry chunk that every page load waits for. See `isNativeApp` in src/server.ts.
VITE_TAXIS_PACKAGED=0
